# Word Guessing Game

A server-side rendered word puzzle game built with Node.js and Express, where players guess a secret word with colored letter feedback.

## Setup & Run
- Install dependencies: `npm install`
- Start server: `node server.js`
- Open browser: `http://localhost:3000`
